# Raw dataset — large (~1.3GB), and re-downloadable from source
# (see backend/data/README.md for the Kaggle link)
backend/data/raw/
backend/data/processed/

# Combined multi-year database (~180MB+, all 6 loaded seasons) — GitHub
# hard-blocks any push containing a file over 100MB, so this can't be
# committed the way the old single-season valorant_test_2025.db was.
# Rebuild it locally: see "Rebuilding the database" in the root README.
backend/data/valorant.db

# Python
__pycache__/
*.pyc
venv/
.venv/
*.egg-info/

# Environment / secrets
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (if/when the real React+Vite frontend gets built)
node_modules/
dist/
.vite/

# Logs
*.log
