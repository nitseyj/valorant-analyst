# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Team logos are already tracked once under frontend/prototype/assets — this
# copy is synced locally (see scripts/sync-team-logos.mjs, runs via
# `npm run predev`/`prebuild`) so the same ~28MB of PNGs isn't committed twice.
public/assets/valorant/teams/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
